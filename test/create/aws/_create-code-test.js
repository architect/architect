var test = require('tape')

var createCode = require('../../../src/create/aws/_create-code')

test('createCode writes code to namespaced + indexed location under project src/ dir if it does not exist', t=> {
  t.end()
})
test('createCode does not write code to namespaced + indexed location under project src/ dir if it does exists', t=> {
  t.end()
})
test('createCode invokes install-workflows-and-data with localpath on every invocation', t=> {
  t.end()
})
