var test = require('tape')

var createTables = require('../../../../src/create/aws/create-tables')

test('create-tables should invoke main table creating module for staging with provided app and table names', t=> {
  t.end()
})
test('create-tables should invoke main table creating module for production with provided app and table names', t=> {
  t.end()
})
