var test = require('tape')

var createTable = require('../../../../src/create/aws/create-tables/_create-table')

test('create-table doesnt create a new table if it already exists in dynamo', t=> {
  t.end()
})
test('create-table waits for table to be active before updating ttl if provided', t=> {
  t.end()
})
