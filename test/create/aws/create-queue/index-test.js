var test = require('tape')

var create = require('../../../../src/create/aws/create-queue')

test('create-queue creates an sqs queue with specified queue name for staging', t=> {
  t.end()
})
test('create-queue creates an sqs queue with specified queue name for production', t=> {
  t.end()
})
