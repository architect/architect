var test = require('tape')

var createDeployments = require('../../../../src/create/aws/create-http-lambda-deployments')

test('create-http-lambda-deployments should create a lambda for staging with provided app and route details if they do not exist', t=> {
  t.end()
})
test('create-http-lambda-deployments should create a lambda for production with provided app and route details if they do not exist', t=> {
  t.end()
})
test('create-http-lambda-deployments should not create a lambda for staging if it does exist', t=> {
  t.end()
})
test('create-http-lambda-deployments should not create a lambda for production if it does exist', t=> {
  t.end()
})
