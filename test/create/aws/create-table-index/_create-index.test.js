var test = require('tape')

var createIndex = require('../../../../src/create/aws/create-table-index/_create-index')

test('create-index should retry if an index is in the status of being deleted', t=> {
  t.end()
})
test('create-index should retry if desired table is not active yet', t=> {
  t.end()
})
test('create-index should skip creating an index if it already exists', t=> {
  t.end()
})
test('create-index should update table index if index does not exist', t=> {
  t.end()
})
