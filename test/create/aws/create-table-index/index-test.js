var test = require('tape')

var createTableIndex = require('../../../../src/create/aws/create-table-index')

test('create-table-index should invoke main index creating module for staging with provided app and index names', t=> {
  t.end()
})
test('create-table-index should invoke main index creating module for production with provided app and index names', t=> {
  t.end()
})
