var test = require('tape')

var createStatic = require('../../../../src/create/aws/create-http-static-deployments')

test('create-static-deployment should create a bucket for staging with provided app and static content names', t=> {
  t.end()
})
test('create-static-deployment should create a bucket for production with provided app and static content names', t=> {
  t.end()
})
