var test = require('tape')

var createRoute = require('../../../../src/create/aws/_create-route')

test('createRoute invokes createResource for staging with route details', t=> {
  t.end()
})
test('createRoute invokes createResource for production with route details', t=> {
  t.end()
})
