var test = require('tape')

var createEvents = require('../../../../src/create/aws/create-events')

test('create-events should create a topic for staging with provided app and event names', t=> {
  t.end()
})
test('create-events should create a topic for production with provided app and event names', t=> {
  t.end()
})
