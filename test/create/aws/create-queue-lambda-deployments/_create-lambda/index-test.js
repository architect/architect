var test = require('tape')

var create = require('../../../../../src/create/aws/create-queue-lambda-deployments/_create-lambda')

test('create-lambda skips creating lambda if it already exists', t=> {
  t.end()
})
test('create-lambda waterfalls iam, read, upload and map', t=> {
  t.end()
})
