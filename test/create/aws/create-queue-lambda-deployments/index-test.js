var test = require('tape')

var create = require('../../../../src/create/aws/create-queue-lambda-deployments')

test('create-queue-lambda-deployments invokes createLambda with app, queue and name parameters for staging', t=> {
  t.end()
})
test('create-queue-lambda-deployments invokes createLambda with app, queue and name parameters for production', t=> {
  t.end()
})
