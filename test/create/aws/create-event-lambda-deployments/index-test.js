var test = require('tape')

var createEventDeployments = require('../../../../src/create/aws/create-event-lambda-deployments')

test('create-event-lambda-deployments should create a lambda for staging with provided app and event names if they do not exist', t=> {
  t.end()
})
test('create-event-lambda-deployments should create a lambda for production with provided app and event names if they do not exist', t=> {
  t.end()
})
test('create-event-lambda-deployments should not create a lambda for staging if it does exist', t=> {
  t.end()
})
test('create-event-lambda-deployments should not create a lambda for production if it does exist', t=> {
  t.end()
})
