var test = require('tape')

var create = require('../../../../src/create/aws/create-scheduled-lambda-code')

test('create-scheduled-lambda-code invokes createCode with schedule details as index parameter', t=> {
  t.end()
})
