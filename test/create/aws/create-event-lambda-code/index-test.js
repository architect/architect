var test = require('tape')

var createEventLambdaCode = require('../../../../src/create/aws/create-event-lambda-code')

test('create-event-lambda-code invokes createCode with event name as index parameter', t=> {
  t.end()
})
