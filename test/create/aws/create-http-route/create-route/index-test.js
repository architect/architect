var test = require('tape')

var createResource = require('../../../../../src/create/aws/create-http-route/create-route')

test('createResource starts a waterfall by creating resources with env and route details', t=> {
  t.end()
})
test('createResource continues waterfall by adding method with route details and previous resource creation details', t=> {
  t.end()
})
test('createResource continues waterfall by setting up request with route details and previous method add details', t=> {
  t.end()
})
test('createResource finishes waterfall by setting up response with route details and previous request setup details', t=> {
  t.end()
})
