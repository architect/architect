//let aws = require('aws-sdk')

module.exports = function _put(params, callback) {

  /*
  let ssm = new aws.SSM
  ssm.putParameter({
    Name: 'STRING_VALUE',
    Value: 'STRING_VALUE',
    Type: 'SecureString',
    Overwrite: true
  }, callback)
  */
  callback(params)
}
